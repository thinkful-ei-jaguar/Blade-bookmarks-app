const store = {
  bookmarks: [...], //Array of bookmark objects shown collapsed
  adding: false, //Unless the error happened while adding
  error: 'error.message', //It will contain the message error generated.  This will similar to how it is in the api shopping list.
  filter: 0 //Unless the error happened while filter was applied
};


